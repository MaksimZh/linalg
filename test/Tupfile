include_rules
DFLAGS += -L-loddsends -L-lblas -L-llapack
DFLAGS += -unittest -version=linalg_backend_lapack

: *.d \
  ../linalg/*.d \
  ../linalg/aux/*.d \
  ../linalg/backends/*.d \
  ../linalg/operations/*.d \
  ../linalg/storage/*.d \
  | $(LIBODDSENDS) |> !dcompile |> test.o
: test.o | $(LIBODDSENDS) |> !build |> test
